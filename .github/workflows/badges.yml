name: Badges

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  update-badges:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Update badge links (noop placeholder)
        run: echo "Badges will be updated after repo rename to buddy"
